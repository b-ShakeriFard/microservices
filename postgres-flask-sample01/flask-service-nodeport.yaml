apiVersion: v1
kind: Service
metadata:
  name: fp-name-db-flask-service-nodeport
  namespace: name-db-lab
spec:
  selector:
    app: fp-name-db-app
  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004
      nodePort: 30100
  type: NodePort
