apiVersion: v1
kind: Service
metadata:
  name: flask-postgres-front-service-nodeport
spec:
  selector:
    app: flask-postgres-front-app
  ports:
    - protocol: TCP
      port: 5008
      targetPort: 5008
      nodePort: 30100
  type: NodePort
